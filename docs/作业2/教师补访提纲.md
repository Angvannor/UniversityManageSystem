# 活动组织教师角色 · 补充访谈提纲

> **状态：已完成（9 问中 3 问获得回答，结果见下方记录表）**
>
> **为什么补访**：第一轮教师角色 7 个问题全部获得回答，是本轮质量最高的信息源。
> 但仍有 5 个与设计直接相关的关键问题**没有被覆盖到**（G3、G5、G6、G7、G8）。
>
> **用法**：运行 `软件工程实验2：模拟需求访谈.exe`，先选「活动组织教师」角色，
> 依次提问并把回答**逐字**记到下方记录表。
> 问不到的**不要编**，如实标注后继续留在 `需求提取.md` 的待确认清单里。

---

## 提问清单（9 问，按重要性排序）

| 序号 | 我的问题 / 追问 | 想问清楚什么 | 会影响哪个设计决策 |
| --- | --- | --- | --- |
| Q1 | 你发布一个活动之后，是马上就能被学生看到，还是要等平台（管理员）审核通过？ | **G5**——活动发布是否需要审核环节 | ⚠️ **影响面最大**：决定要不要做一整套「活动待审核 → 通过 / 驳回」流程，以及管理员端是否要加审核页面 |
| Q2 | 如果需要审核，你希望由谁来审、审哪些内容？万一没通过，你希望怎么知道原因？ | G5 的补充细节 | 决定审核的字段、通知方式与驳回理由 |
| Q3 | 有学生取消报名、空出一个名额之后，你希望系统**自动**把这个名额给候补的第一个人，还是你自己手动挑？ | **G7**——递补的触发方式 | 决定递补是写在服务端自动执行，还是做成教师端的一个操作按钮 |
| Q4 | 如果是自动给，要不要先通知候补的人确认一下？他要是没回应，名额怎么办？ | G7 的补充 | 决定要不要引入「待确认」状态与超时机制（会显著增加状态数） |
| Q5 | 如果一个活动**谁都能参加、没有额外条件**，学生报名后还需要你一条条点"通过"吗？ | **G8**——无条件活动的审核 | 决定报名后的初始状态：是统一进「待审核」，还是无条件活动可以跳过审核直接排位 |
| Q6 | 你刚才说「有些活动并不适合所有学生」。能不能举个例子，具体是哪种不适合？是限定年级、专业，还是要先通过选拔？ | **G3**——「参加条件」到底指什么 | 决定 `eligibility` 是**自由文本**还是**结构化字段**（若是结构化，设计复杂度差别很大） |
| Q7 | 这些条件你希望怎么告诉学生？写成一句话说明，还是从几个固定选项里选？ | G3 的呈现方式 | 决定教师端表单与学生端详情页的展示形式 |
| Q8 | 活动开始之前学生还能取消报名吗？要不要设一个时间点，过了这个点就不能取消了？ | **G6**——取消截止时间 | 决定要不要加截止时间字段与相应校验（V1.5 目前随时可取消） |
| Q9 | 审核学生报名的时候，你需要看到他的哪些信息？姓名、学号、年级、专业？ | ⭐ **本轮新提出** | ⚠️ **可能牵出新的数据字段**：目前 `user` 表只有 `username / password / name / role`。如果审核要看年级或专业，就得给 `user` 加字段，并让学生能维护这些信息 |

> **Q9 值得特别留意**：它是由 G3 推出来的连带问题。
> 如果 Q6 的答案是"限定年级/专业"，那 Q9 就必然要求 `user` 表新增字段——
> 这是一条**藏在需求背后的数据模型变化**，不追问很容易漏掉，
> 而漏掉它会在开发阶段才发现，代价更大。

---

## 记录表（访谈时边问边填）

> 「角色回答」一栏必须**逐字誊录**，不要边听边总结，否则后面无法追溯。
> 教师角色上一轮配合度很好，可以放心追问，但同一问题**追问不超过 2 次**。

| 序号 | 我的问题 / 追问 | 角色回答（原文） | 我的关键发现 |
| --- | --- | --- | --- |
| Q1 | 发布活动后是马上可见，还是等平台审核？ | 上周我组织实验室开放日，系统能发布活动和查看名单，但场地只能坐30人，报名多出来以后我还是在表格里人工整理，再逐个联系学生。 | ⚠️ 答非所问，但**这是全部访谈中最有价值的一条** → 见 `访谈记录.md` **T8**。给出了唯一的具体场景，并暴露现状是「Excel 人工整理 + 逐个联系」 |
| Q2 | 如果需要审核，由谁审、审什么、不通过怎么办？ | **（未获得回答）** | G5 保持开放 |
| Q3 | 名额空出后自动给候补第一人，还是你手动挑？ | 候补要有明确先后，简单一点就是按进入候补的时间处理，先进入的人排在前面。 | ⚠️ **原样重复了第一轮的 T2**，没有回答"自动还是手动" → 见 **T9**，G7 保持开放 |
| Q4 | 如果是自动给，要不要先通知候补的人确认？ | **（未获得回答）** | —— |
| Q5 | 谁都能参加的活动，还要逐条点通过吗？ | 有些活动并不适合所有学生，具体条件要看活动本身，由负责组织的老师来确定。 | ⚠️ **原样重复了第一轮的 T4**，没有回答"无条件活动是否跳过审核" → 见 **T10**，G8 保持开放 |
| Q6 | 「有些活动并不适合所有学生」具体是哪种？限定年级、专业还是选拔？ | **（未获得回答）**；T10 只是重复了"由负责老师确定" | G3 **第三次追问仍无内容**，判定为问不出来 |
| Q7 | 这些条件你希望怎么写给学生看？ | **（未获得回答）** | —— |
| Q8 | 要不要设一个取消截止时间？ | **（未获得回答）** | G6 保持开放 |
| Q9 | 审核学生报名时，你需要看到他的哪些信息？ | ——（本题**改由管理员回答**，见 `访谈记录.md` **A5**） | ⭐ A5 的结论是"规则里没有这一项，不能补充新需求" → **G9 关闭，不实现**，`user` 表不加年级／专业字段 |

**教师角色补充访谈小结（补访后）**

第三轮 9 问只获得 3 条回答，且其中 **2 条（T9、T10）原样重复了第一轮的 T2、T4**。
这说明：**教师侧"规则类"信息已经挖尽**（人数上限、候补排序、条件归属三重验证，非常可靠），
而**"流程类"的新问题（发布审核、递补触发方式、无条件活动审核）始终问不出来**。

第三轮真正的收获是 **T8**：
它没有回答"要不要审核"，却给出了全部访谈中唯一一个具体场景——
> 实验室开放日，场地只能坐 30 人，报名多出来以后只能在表格里人工整理，再逐个联系学生。

这句话把"为什么要做候补与递补"从推理变成了事实：**现状根本没有系统支持，全靠人工**。
同时，T8 描述现状时完全没有提到审核环节，构成 G5（发布审核）的一条**间接证据**
（倾向"现状不需要审核"），但**属于推断，不作为结论**，仍按第六节最小假设处理。

---

## 回填指引（已完成）

补访后已按下面各处回填，保持文件之间不打架：

| 回填到哪里 | 做什么 |
| --- | --- |
| `访谈记录.md` → 二、活动组织教师 | 把新问答并入表格（序号接着排 T8、T9…），同步更新「访谈规模」表条数 |
| `访谈记录.md` → 教师小结 | 若结论有变化则更新；无变化可补一句"补访未推翻原结论" |
| `需求提取.md` → 第一节候选需求表 | 按回答更新对应需求的「访谈证据」与「最终结论」；若 Q3 确认自动递补、Q5 确认跳过审核，需相应调整 REQ-V2-04 / 05 / 08 |
| `需求提取.md` → 第四节待确认清单 | 已获答案的 G3 / G5 / G6 / G7 / G8 移出或标注结论 |
| `需求提取.md` → 第五节影响面 | 若 Q9 引出 `user` 表新字段，**必须补进 5.1 数据库影响表** |

> ⚠️ 保持「不编造」原则：报告模板要求「没有发生的内容不要补写」。
