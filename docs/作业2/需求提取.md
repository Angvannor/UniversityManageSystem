# 实验二 · 需求提取与澄清（候选需求）

> **用途**：实验报告**第四节「需求提取与澄清」、第五节「角色与权限边界」、第七节「影响分析」**的素材。
>
> **原则**：每一条候选需求都必须能追溯到访谈原文（见 `访谈记录.md` 的编号 S1~S2 / T1~T7 / A1~A4）。
> 无访谈证据支撑的内容一律不写。
>
> ⚠️ **本文件写的是「候选需求」，还不是正式需求。**
> 报告模板明确要求：「AI提出但未被访谈证据确认的内容，不应直接作为正式需求。」
> 因此下表「最终结论」一栏需要由本组逐条确认后才生效。

---

## 一、候选需求清单（对应报告第四节）

| 编号 | 访谈证据 / 来源角色 | 候选需求或规则 | 是否需进一步确认 | 最终结论（待本组确认） |
| --- | --- | --- | --- | --- |
| REQ-V2-01 | T1、T3、S2 | 每个活动必须能设置**可接待人数上限**；上限由该活动的负责教师设定，依据场地／设备／组织条件，活动之间互相独立 | 否（教师两处 + 学生一处，三方印证） | 建议采纳 |
| REQ-V2-02 | T1、T6 | 必须区分**报名人数**与**正式参加人数**，人数上限约束的是「正式参加人数」 | 否 | 建议采纳 |
| REQ-V2-03 | T4、T3、S1、**S3**、**S4** | 每个活动由负责教师填写**参加条件**，并让**学生可见**（学生目前完全不知道按什么条件判断） | 是（「条件」的具体内容仍未知，见 G3） | 建议采纳，字段形式待定；**已确认必须显示在活动详情页**（S3：学生只靠「列表 → 详情页」获取信息） |
| REQ-V2-04 | T6、S1、A3 | 存在**报名审核**环节：教师判定报名是否符合参加条件 | 否 | 建议采纳 |
| REQ-V2-05 | T6 | ⭐ **「审核通过 ≠ 正式参加」**：审核通过与名额分配是两个独立环节，通过之后仍要看当时有没有位置 | 否 | **必须采纳**（本轮核心模型变化） |
| REQ-V2-06 | T2、S2、**S4**、**S5** | 超出上限的学生进入**候补队列**，不是直接被拒绝 | 否（四方证据） | 建议采纳 |
| REQ-V2-07 | T2、**S5** | 候补按**进入候补的时间**排序，先进入者在前（FIFO） | 否（教师与学生两方一致） | 建议采纳。**已精化**：S5 表明学生只要求「知道自己是不是还在等待」，并未要求精确位次，因此界面展示候补状态即可，不必暴露排位 |
| REQ-V2-08 | T2、T6、T5 | 名额空出时按候补顺序处理（递补） | 是（自动递补还是教师手工，见 G7） | 建议采纳，触发方式待定 |
| REQ-V2-09 | T1 | 教师端能**随时看到**该活动「已确定参加」的人数 | 否 | 建议采纳 |
| REQ-V2-10 | T5、**S6** | 保留**取消报名**功能；取消后名单与人数**即时更新**，保证教师看到准确的名单 | 否（教师与学生两方一致） | 确认保留（V1.5 已有，需补强统计准确性） |
| REQ-V2-11 | T7 | 教师只能修改／关闭／删除**自己发布**的活动；其他教师的活动**可以浏览但不能改** | 否 | 确认保留（V1.5 已有 `checkOwnership`） |
| REQ-V2-12 | A1、A2、A4 | 新增**系统管理员**角色 | 否 | 建议采纳 |
| REQ-V2-13 | A1 | 管理员可**查看平台全部活动**，用于监督违规或明显错误的信息 | 否 | 建议采纳 |
| REQ-V2-14 | A2、A4 | 管理员可**查看平台全部用户账号及其状态** | 否 | 建议采纳 |
| REQ-V2-15 | A2 | 管理员可在发现账号明显异常时**停用**账号 | 否 | 建议采纳 |
| REQ-V2-16 | A2 | 管理员可在问题处理后**恢复**账号为可用状态 | 否 | 建议采纳 |
| REQ-V2-17 | A1、A4 | 管理员**不得**代替教师处理具体报名、**不得**审核报名（权限边界，不是功能） | 否（两条独立证据互证） | **必须采纳** |
| REQ-V2-18 | S4、S5 | 学生能查看自己在该活动中的**报名／候补状态**（是否已通过、是否还在等待中） | 否 | 建议采纳（S4 表明学生完全不知道有人退出后自己能否补进去）。**第二轮补访新增** |

> **统计**：18 条候选需求中，14 条证据充分可直接采纳，2 条需补充确认（REQ-V2-03、REQ-V2-08），
> 2 条属于权限边界约束（REQ-V2-05、REQ-V2-17）。
>
> **第二轮学生补访带来的变化**：新增 REQ-V2-18；
> REQ-V2-06 / 07 / 10 由「教师单方陈述」升级为「教师与学生两方一致」；
> REQ-V2-03 明确了实现位置（活动详情页）；REQ-V2-07 的范围被收窄（只需状态，不需位次）。

---

## 二、角色与权限边界（对应报告第五节）

| 角色 | 允许的主要操作 | 明确不能执行的操作 | 依据 |
| --- | --- | --- | --- |
| **学生** | 浏览活动列表与详情；查看活动的参加条件；报名；查看自己的审核结果与候补状态；取消报名；查看我的报名 | 发布／修改／删除活动；查看他人的报名名单；自行决定自己是否通过；停用账号 | S1、S2、T5、T7 |
| **活动组织教师** | 发布活动；设定人数上限；填写参加条件；修改／关闭／删除**自己发布**的活动；审核报名；查看**自己活动**的报名名单与已确定参加人数；按候补顺序处理名额 | 修改其他教师发布的活动；代替管理员停用账号；查看自己活动之外的报名名单 | T1、T2、T3、T4、T6、T7 |
| **系统管理员** | 查看平台全部活动（监督用途）；查看全部用户账号及状态；停用账号；恢复账号 | 处理具体报名；审核报名；代替教师决定参加条件 | A1、A2、A4 |
| 系统管理员（**待确认**） | 是否负责**审核教师发布的活动**——A3 未正面回答，无法判定 | —— | A3 |

**边界结论**：三个角色的职责可以概括为——
**教师定规则、学生受规则约束、管理员只保证账号与内容不出问题，不介入业务判定。**
其中最稳定的一条是管理员的边界（A1 与 A4 两条独立回答互相印证），
最模糊的一条也正是管理员与教师之间那条线（A3）。

---

## 三、跨角色核对与需求冲突（对应报告第四、十二节）

| 编号 | 冲突 / 张力 | 涉及证据 | 分析 |
| --- | --- | --- | --- |
| C1 | **活动发布是否需要平台审核？无人负责** | A1 说管理员要「监督违规或明显错误的信息」，A3 却把问题推给教师 | 如果活动发布**不经**平台审核，管理员的「监督」就只能是**事后**发现，无法预防。这是一处真实的**需求空白**，需回访教师确认 |
| C2 | **规则由教师掌握，后果由学生承担** | T4「由负责组织的老师来确定」 vs S1「我并不清楚」、S2「报不上」、S4「不太清楚自己还能不能补进去」、S3（学生只靠列表与详情页获取信息） | 教师单方面定条件，学生却看不到条件——信息严重不对称。补访进一步确认：学生的**唯一信息入口就是活动详情页**（S3），因此条件的可见性只能由详情页承载。这是 REQ-V2-03 与 REQ-V2-18 的直接依据 |
| C3 | **「限制」有两种，教师只说了容量** | T3 把「资格限制」理解为人数上限；T4 承认「有些活动并不适合所有学生」 | 说明限制实际有两类：**容量限制**（最多几人）与**适合性限制**（谁适合参加）。设计时若只做容量，会丢掉后者 |
| C4 | **取消是否需要截止时间** | T5「最好尽早处理，让名单保持准确」 | 「尽早」是对学生的期望，但是否要设**取消截止时间**教师没有明确要求，属于推断，不能直接写成需求 |
| C5 | **候补与「三方互证」** | T2 候补排序 + S2 报不上 + T6 通过≠参加 | 三条独立证据共同支撑候补机制，是本轮证据链最完整的一组需求 |

---

## 四、待确认问题清单（下次访谈 / 跨角色核对用）

> 这 7 个问题都是**访谈中没有得到答案**的，报告第四节「是否需进一步确认」一栏即由此而来。
> 建议优先补访 **学生**（仅 2 条有效回答，覆盖面最弱）。

| 编号 | 尚未明确的问题 | 来源 | 建议询问对象 | 建议问法 |
| --- | --- | --- | --- | --- |
| G1 | 平台账号由谁创建？ | A1 **未回答** | 系统管理员 | 「一个新学生第一次使用系统，他的账号是系统自动开通、管理员统一导入，还是自己注册？」 |
| G2 | ~~学生通过什么渠道知道有哪些活动？~~ | ✅ **第二轮补访已确认（S3）** | —— | 学生只靠平台的**活动列表 → 详情页**自己看，没有群通知也没有推送。该问题已关闭 |
| G3 | 「参加条件」具体指什么？怎么判定？ | T3 把资格理解成了容量；**学生侧两轮追问（S1、S4）均未获得答案**——学生对该规则的认知是空白的 | 活动组织教师（学生侧已确认问不出，不必再试） | 「除了人数，还有没有别的门槛？比如限定年级、专业、是否通过选拔？」 |
| G4 | 学生毕业、教师离职后账号怎么处理？ | A2 答的是「异常」而非毕业/离职 | 系统管理员 | 「正常毕业和离职的账号，是停用、保留还是删除？历史报名记录要留着吗？」 |
| G5 | 教师发布的活动需要平台审核吗？ | A3 **未回答** | 活动组织教师 + 跨角色核对管理员 | 「活动发布后是直接可见，还是要等管理员审核通过？」 |
| G6 | 是否需要报名／取消的截止时间？ | T5 只说「尽早」 | 活动组织教师 | 「活动开始前还能取消吗？要不要设一个截止时间自动关闭报名？」 |
| G7 | 名额递补是自动还是教师手工处理？ | T2 未说明触发方式 | 活动组织教师 | 「有人退出后，是系统自动按顺序通知候补的人，还是你自己去点？」 |
| G8 | 没有设置参加条件的活动，报名还要教师逐条审核吗？ | T4、T6 都默认「有条件」；无条件活动的情形未被覆盖 | 活动组织教师 | 「如果这个活动谁都能参加，报名是直接算数，还是要你一条条点通过？」 |
| G9 | 教师审核报名时，需要看到学生的哪些信息？（姓名／学号／年级／专业） | ⭐ **由 G3 推导出的连带问题**——若参加条件包含年级或专业，`user` 表就必须新增字段 | 活动组织教师 | 「审核的时候你要看学生的什么信息才能判断他符不符合条件？」 |
| G10 | 账号被停用后，该学生**已有的报名记录**如何处理？ | ⭐ **由 REQ-V2-15 推导出的连带问题**——A2 只说可停用，未说停用后的连带影响 | 系统管理员 | 「停用某个学生之后，他之前报名的活动怎么办？要不要一并取消？」 |

> **当前开放缺口 9 条**：G1、G3、G4、G5、G6、G7、G8、G9、G10（G2 已于第二轮学生补访关闭）。
> 其中 **G5（发布审核）、G7（递补方式）、G8（无条件活动审核）、G1（账号创建）** 会直接影响设计决策，
> 优先级最高；G9、G10 属于连带影响，容易漏但漏了要到开发阶段才暴露。
> 第三轮补访提纲见 `教师补访提纲.md` 与 `管理员补访提纲.md`。

---

## 五、对 V1.5 的影响面（对应报告第七节）

### 5.1 数据库（三张表都要动）

| 表 | V1.5 现状（`db/schema.sql`） | V2.0 需要的改动 |
| --- | --- | --- |
| `user` | `role` 只有 `STUDENT` / `TEACHER`；**没有状态字段** | `role` 增加 `ADMIN`（REQ-V2-12）；新增 `status`（`ACTIVE` / `DISABLED`）支撑停用与恢复（REQ-V2-15、REQ-V2-16） |
| `activity` | 无人数、无条件字段；`status` 已定义 `OPEN` / `CLOSED` / `FINISHED` | 新增 `capacity`（人数上限，REQ-V2-01）与 `eligibility`（参加条件，REQ-V2-03） |
| `activity_registration` | `status` 只有 `REGISTERED` / `CANCELLED` 两态 | `status` 扩展为多态（待审核→未通过／条件通过→候补／正式参加）；新增记录**进入候补时间**的字段（REQ-V2-07） |

**一个可以复用的点**：`uk_reg_student_activity (activity_id, student_id)` 唯一约束
在候补模型下依然成立——一个学生在同一活动上**始终只有一行记录**，状态流转都改这一行。
这与 V1.5 中「取消报名改 `status` 而不删记录、重新报名复用原行」的做法完全一致，
**因此唯一约束不需要改**。这一点可以直接写进报告作为「V1.0 的数据设计经得起需求变化」的证据。

### 5.2 后端

| 类别 | 内容 |
| --- | --- |
| **新增** | `AdminApi`（全部活动总览、用户列表、停用／恢复账号）；`RegistrationService` 的审核、候补、递补、正式参加人数统计 |
| **改造** | `ActivityService`（`capacity` / `eligibility` 的录入与校验）；`RequestContext.requireRole`（支持 `ADMIN`）；`AuthApi` / `SessionManager`（被停用账号不得登录） |
| **不受影响** | `DBUtil`、`PasswordUtil`、`JsonUtil`、`ApiRouter` 的路由匹配与统一响应机制、DAO 的参数化 SQL 写法与资源关闭方式 |

### 5.3 前端

| 类别 | 内容 |
| --- | --- |
| **新增** | 管理员端 2 个页面：活动总览（监督）、用户管理（停用／恢复） |
| **改造** | 教师端：活动管理表单增加「人数上限」「参加条件」；报名名单按 **待审核 / 候补（含位次）/ 正式参加** 分组展示 |
| **改造** | 学生端：活动详情显示**参加条件**与自己的报名状态；我的报名显示审核结果与**是否仍在候补等待中**（按 S5，只需状态、无需精确位次） |
| **改造** | `router/index.js` 守卫与 `App.vue` 侧边菜单增加 `ADMIN` 分支 |
| **不受影响** | `api/request.js` 的拦截与错误码处理、`stores/auth.js` 主体逻辑、`styles/global.css` 布局 |

### 5.4 总体影响说明（可直接写入报告的段落草稿）

> 本轮需求变化集中在**报名流程的状态模型**上，而不是全新的业务领域。
> V1.5 把「报名」实现为一个两态记录（已报名／已取消），
> 而访谈结论（T6）要求「审核通过」与「正式参加」分离，T2 又要求候补按时间排队，
> 因此报名记录需要从两态扩展为一条带顺序的状态链。
> 相应地，数据库三张表都要增量修改，业务层与界面层需要较大调整。
> 但**分层结构本身没有被推翻**：数据访问层的 SQL 封装、统一响应格式、
> 令牌与角色校验机制、以及 V1.0 已确认的六项业务规则全部继续有效，
> 说明 V1.0 的分层设计对需求演化是友好的。
> 新增的「系统管理员」角色也验证了这一点——它只是新增一条界面分支与一组接口，
> 没有导致任何已有代码重写。

---

## 六、下一步（建议顺序）

1. **本组先逐条确认第四节表格的「最终结论」** —— 这是把候选需求变成正式需求的唯一合法途径；
2. 若时间允许，按「待确认问题清单」补访 **学生**（覆盖面最弱）与 **教师**（确认 G5、G6、G7）；
3. 确认之后，再写**第六节「V2.0 用户故事与验收标准」** —— 现在先不写，
   因为模板要求「AI提出但未被访谈证据确认的内容，不应直接作为正式需求」，
   在需求尚未定稿前生成用户故事，等于把未经确认的内容包装成正式需求；
4. 最后按影响面动手改代码，并同步做回归测试（V1.0 的六项需求不得失效）。

---

## 附：本步骤的 AI 使用说明（供报告第十一节填写）

| 使用环节 | AI 主要帮助内容 | 本组需要如何检查／修改 |
| --- | --- | --- |
| 访谈记录整理 | 把截图中的问答逐字誊录为表格，并按报告模板补出「我的关键发现」草稿 | 需核对誊录是否与原话一致；「关键发现」需本组判断是否成立 |
| 需求提取 | 从访谈原话中提炼候选需求、归纳角色权限边界、比对跨角色冲突、列出待确认问题 | 需逐条确认「最终结论」；AI 标记为「待确认」的不得直接当结论 |
| 影响分析 | 对照 `db/schema.sql` 与现有代码，列出受影响的表、模块与页面 | 需在真正动手改代码时复核，改完以实际代码为准 |

> **声明**：需求本身来自访谈，AI 只参与了整理、归纳与影响面梳理。
> AI 的任何推断都已在本文中以「待确认」或「推断」标出，未作为正式需求。
